/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number}
 */
var findMaxAverage = function (nums, k) {
  let res = 0;
  for (let i = 0; i < k; ++i) {}
};

findMaxAverage([1, 12, -5, -6, 50, 3], 4);
